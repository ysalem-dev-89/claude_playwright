# Required for every run — this script is AI-first: the first time it sees a job source, and
# any time it falls back to repair a broken selector, it calls this model.
ANTHROPIC_API_KEY=

# Optional: override the model used for parsing/filling.
# STAGEHAND_MODEL=claude-3-7-sonnet-latest
